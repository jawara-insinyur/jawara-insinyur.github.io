import SectionTitle from "../components/typography/SectionTitle";

export default function OurWeapons({ className }: { className?: string }) {
  return (
    <div id="ourweapons" className={className}>
      <SectionTitle>Our Weapons</SectionTitle>
    </div>
  );
}
