function SectionTitle({ children }: { children: React.ReactNode }) {
  return (
    <div className="mb-[70px] text-center text-5xl font-bold text-white">
      {children}
    </div>
  );
}

export default SectionTitle;
